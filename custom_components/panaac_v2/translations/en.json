{
  "title": "PanaAC v2 (MQTT)",
  "entity": {
    "climate": {
      "remote_controller": {
        "name": "Remote Controller (v2)"
      }
    }
  },
  "config": {
    "step": {
      "user": {
        "title": "Configure PanaAC v2",
        "description": "Enter a device name and the MQTT topic prefix used by the ESPHome device.",
        "data": {
          "device_name": "Device name",
          "topic_prefix": "MQTT topic prefix"
        }
      }
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  }
}
