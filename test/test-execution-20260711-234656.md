# PanaAC v2 test execution

Timestamp: `2026-07-11T23:46:56+07:00`

## Summary

- Overall: `fail`
- Pass: `24`
- Fail: `12`
- Skip: `5`
- Blocked: `0`

## What passed

- All ESPHome config/compile checks for `C1`, `C2`, `C3`, `C4`, `C5`, `C6`, and `C3-automation`.
- HA command round-trip checks for `set_hvac_mode`, `set_temperature`, `set_fan_mode`, `set_swing_mode`, `set_swing_horizontal_mode`, `turn_on`, `turn_off`, and `toggle`.
- HA `hvac_action` derivation for `off`, `cool`, `heat`, `dry`, `fan_only`, and auto cooling/heating cases.

## Main failures

- HA Group 1 retained-traits expectations did not match the currently loaded climate entity capabilities.
- HA Group 2 cold-start availability/state expectations did not match the current entity state.
- HA Group 2 auto idle retained `heating` instead of `idle`.
- HA retained-resilience check did not drop to `["off"]` after traits removal.
- HA Group 3 trigger/condition coverage still needs runner work; the latest completed run failed the Group 3 pytest invocation because `pytest` was not available via the direct venv python path.

## Notes

- Report artifact: `/tmp/panaac_v2_runner_fix6/report.md`
- Raw logs: `/tmp/panaac_v2_runner_fix6/ha.log`
- Captures: `/tmp/panaac_v2_runner_fix6/captures/`

