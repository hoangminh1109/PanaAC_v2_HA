# PanaAC v2 HA test results

Timestamp: `2026-07-11 22:11:27 +07`

Branch at execution time: `testing/full-test`

Observed HA entity: `climate.esphome_panaac_v2_remote_controller_v2`

Observed topic prefix: `panaac_v2/esphome-panaac-v2`

## Summary

- HA core was updated to current `dev` and run successfully on `http://127.0.0.1:8123`.
- The climate Labs flag requirement is gone on this build.
- MQTT round-trip, derived `hvac_action`, and HA climate building-block automations were exercised successfully.
- The v1/native-only ESPHome variant `C4` is not applicable to this HA custom-MQTT integration path.

## Group 1 - Traits consistency

### 1.1 Before first traits

Pass.

With retained `traits` deleted and HA restarted, the live HA snapshot showed:

- `state=off`
- `hvac_modes=["off"]`
- `fan_modes=null`
- `swing_modes=null`
- `swing_horizontal_modes=null`
- `supported_features=1` (`TARGET_TEMPERATURE` only)

### 1.2 Per variant

Partial pass.

- `C1`: synthetic retained `traits` were adopted immediately by HA. Live snapshot showed `hvac_modes=["off","cool","dry","auto"]`, `fan_modes=["Auto","Level 1","Level 3","Level 5"]`, no horizontal swing, `supported_features=425`.
- `C2`: synthetic retained `traits` were adopted immediately by HA. Live snapshot showed `hvac_modes=["off","cool","heat","dry","auto"]`, `fan_modes=["Auto","Level 1","Level 3","Level 5"]`, no horizontal swing, `supported_features=425`.
- `C3`: live DUT retained `traits` and HA entity capabilities matched the expected full-feature set. `supported_features=937`.
- `C4`: not executed on the HA custom-MQTT path because this ESPHome variant is v1/native and does not use the `<prefix>/traits` contract.
- `C5`: synthetic retained `traits` were adopted immediately by HA. Live snapshot showed `hvac_modes=["off","cool","dry","auto"]`, `fan_modes=["Auto","Level 1","Level 2","Level 3","Level 4","Level 5"]`, horizontal swing present, `supported_features=937`.
- `C6`: synthetic retained `traits` were adopted immediately by HA. Live snapshot showed `hvac_modes=["off","cool","heat","dry","auto"]`, `fan_modes=["Auto","Level 1","Level 3","Level 5","Quiet"]`, no horizontal swing, `supported_features=425`.

Limitations:

- The live DUT remained active on the same prefix, so a stable HA-side proof of `temp_step=1.0` for `C1` and `C5` could not be isolated from the device reasserting its own retained/runtime state.
- The live HA snapshot did not expose `target_temperature_step`; entity-registry reads remained at `0.5` because the DUT on the same prefix reasserted C3 state before a stable isolated readback could be completed.

## Group 2 - Two-way MQTT with the ESPHome side

### 2.1 Device -> HA

Pass.

- Publishing retained `availability=offline` made the entity state `unavailable`.
- Publishing retained `availability=online` restored the entity and previous attributes.
- Publishing retained `state` updated `state`, `hvac_action`, `temperature`, `current_temperature`, `fan_mode`, `swing_mode`, and `swing_horizontal_mode` as expected.

Representative observed HA snapshot after a retained cool-state publish:

```json
{
  "state": "cool",
  "hvac_action": "cooling",
  "temperature": 24,
  "current_temperature": 27,
  "fan_mode": "Level 2",
  "swing_mode": "Middle",
  "swing_horizontal_mode": "Left",
  "supported_features": 937
}
```

### 2.2 HA -> device round-trip

Pass for payload generation.

Observed `<prefix>/set` payloads emitted by HA climate actions:

- `climate.set_hvac_mode(cool)` -> `{"mode": "cool"}`
- `climate.set_temperature(24)` -> `{"target_temperature": 24.0}`
- `climate.set_temperature(24, hvac_mode=cool)` -> `{"target_temperature": 24.0, "mode": "cool"}`
- `climate.set_fan_mode("Level 2")` -> `{"fan_mode": "Level 2"}`
- `climate.set_swing_mode("Middle")` -> `{"swing_mode": "Middle"}`
- `climate.set_swing_horizontal_mode("Left")` -> `{"swing_horizontal_mode": "Left"}`
- `climate.turn_on()` -> `{"mode": "heat"}`
- `climate.turn_off()` -> `{"mode": "off"}`
- `climate.toggle()` from off -> `{"mode": "heat"}`

### 2.3 Derived hvac_action

Pass.

Observed HA snapshots:

- `mode=off` -> `hvac_action=off`
- `mode=cool` -> `hvac_action=cooling`
- `mode=heat` -> `hvac_action=heating`
- `mode=dry` -> `hvac_action=drying`
- `mode=fan_only` -> `hvac_action=fan`
- `mode=auto`, `current_temperature=28`, `target_temperature=24` -> `hvac_action=cooling`
- `mode=auto`, `current_temperature=20`, `target_temperature=24` -> `hvac_action=heating`
- `mode=auto`, `current_temperature=24`, `target_temperature=24` -> `hvac_action=idle`

### 2.4 Availability and retained resilience

Pass for retained-topic behavior on the integration path.

- Deleting retained `traits` and restarting HA returned the entity to conservative defaults until traits were republished.
- Retained `availability` offline/online transitions worked as expected.

Not executed:

- Stopping the broker process itself was not required once the retained availability/unavailability path had been verified.

## Group 3 - Automation

### 3.4 HA-side climate building-block automations

Pass.

Observed MQTT evidence from temporary HA test automations:

- `off -> cool` published:
  - `panaac_v2/test/is_cooling fired`
  - `panaac_v2/test/is_hvac_mode_cool fired`
  - `panaac_v2/test/started_cooling fired`
- `off -> heat` published:
  - `panaac_v2/test/is_heating fired`
  - `panaac_v2/test/started_heating fired`
- `off -> dry` published:
  - `panaac_v2/test/is_drying fired`
  - `panaac_v2/test/started_drying fired`

This was executed on updated HA core with no Labs flag enabled.

Note:

- The current YAML syntax for `climate.is_hvac_mode` on this HA build is:

```yaml
conditions:
  - condition: climate.is_hvac_mode
    target:
      entity_id: climate.some_entity
    options:
      hvac_mode: cool
```

### 3.1 / 3.2 / 3.3 ESPHome automation observed through HA

Blocked.

- The currently reachable lab device did not expose the `C3-automation` template buttons required by the ESPHome automation plan.
- No direct DUT debug log stream was available from this HA-side execution to prove `on_control` / `on_state` ordering.

## Final state left after test

- HA config restored to `config/automations.yaml = []`
- HA restarted successfully and serving on `127.0.0.1:8123`
- MQTT retained `availability=online`
- The live DUT had reasserted retained `state` after test cleanup

