# Stubbed HA pytest execution — 2026-07-12 16:26:43 +07:00

## Scope

- Added offline/stubbed pytest coverage for HA-side climate entity behavior.
- Verified subscriptions, conservative defaults, traits adoption, state ingestion,
  derived `hvac_action`, command publishing, turn on/off, and invalid payload handling.

## Command

```bash
python3 test/run_stubbed_pytest.py --group all
```

## Result

- Status: `pass`
- Summary: `22 passed, 1 warning in 0.45s`
- Warning:
  - `DeprecationWarning` from `homeassistant.components.http.HomeAssistantApplication`

## Notes

- The stubbed pytest suite runs through `ha/core/.venv/bin/uv run --with-requirements requirements_test.txt pytest ...`
  with cache redirected to `/tmp`.
- This complements the existing HIL-oriented `run_full_test.py` flow by covering
  HA Group 2 behavior without requiring the live dev instance or DUT timing.
